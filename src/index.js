import "babel-polyfill";
import React from "react";
import ReactDOM from "react-dom";

import "../public/styles.css";

const Main = (props) => {
  return (
<h1>Charana Reagan</h1>
  );
};

ReactDOM.render(<Main />, document.getElementById("root"));
